// Generated by CoffeeScript 1.10.0
var clientSlider,
  extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
  hasProp = {}.hasOwnProperty;

clientSlider = (function(superClass) {
  extend(clientSlider, superClass);

  function clientSlider() {
    return clientSlider.__super__.constructor.apply(this, arguments);
  }

  clientSlider.prototype.el = '#our-clients-slider-container';

  clientSlider.prototype.start = function() {
    this.next = $('.next', this.$el);
    this.prev = $('.prev', this.$el);
    this.slider = $('#our-clients-slider', this.$el);
    if ((bowser.msie && parseInt(bowser.version)) <= 9 || (bowser.opera && parseInt(bowser.version) <= 12)) {
      return this.slider.bxSlider({
        responsive: true,
        minSlides: 5,
        maxSlides: 5,
        slideWidth: 220,
        preloadImages: 'all',
        pager: false,
        controls: true,
        nextSelector: this.next,
        prevSelector: this.prev,
        nextText: '',
        prevText: '',
        auto: true,
        pause: 5000,
        speed: 400,
        moveSlides: 1,
        buildPager: function() {
          return '';
        }
      });
    } else {
      return this.slider.bxSlider({
        responsive: true,
        minSlides: 5,
        maxSlides: 5,
        slideWidth: 220,
        easing: 'ease-in-out',
        preloadImages: 'all',
        pager: false,
        controls: true,
        nextSelector: this.next,
        prevSelector: this.prev,
        nextText: '',
        prevText: '',
        auto: true,
        pause: 5000,
        speed: 400,
        moveSlides: 1,
        buildPager: function() {
          return '';
        }
      });
    }
  };

  return clientSlider;

})(BB.Widget);
