// Generated by CoffeeScript 1.10.0
var headPhoneDropdown,
  extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
  hasProp = {}.hasOwnProperty;

headPhoneDropdown = (function(superClass) {
  extend(headPhoneDropdown, superClass);

  function headPhoneDropdown() {
    return headPhoneDropdown.__super__.constructor.apply(this, arguments);
  }

  headPhoneDropdown.prototype.el = '#head-contacts';

  headPhoneDropdown.prototype.events = {
    'click .dropdown a': 'changePhone'
  };

  headPhoneDropdown.prototype.start = function() {};

  headPhoneDropdown.prototype.changePhone = function(e) {
    var number, region, target;
    e.preventDefault();
    console.log('clicked');
    target = $(e.currentTarget);
    region = target.text();
    number = target.attr('data-number');
    $('.region', this.$el).text(region);
    $('.number', this.$el).text(number);
    return $('.number', this.$el).attr("href", "tel:" + (number.replace(/[^0-9]/, "")));
  };

  return headPhoneDropdown;

})(BB.Widget);
