// Generated by CoffeeScript 1.12.4
var mainSlider,
  extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
  hasProp = {}.hasOwnProperty;

mainSlider = (function(superClass) {
  extend(mainSlider, superClass);

  function mainSlider() {
    return mainSlider.__super__.constructor.apply(this, arguments);
  }

  mainSlider.prototype.el = '#main-slider-container';

  mainSlider.prototype.debug = false;

  mainSlider.prototype.start = function() {
    this.next = $('.next', this.$el);
    this.prev = $('.prev', this.$el);
    this.slider = $('#main-slider', this.$el);
    if ((bowser.msie && parseInt(bowser.version)) <= 9 || (bowser.opera && parseInt(bowser.version) <= 12)) {
      return this.slider.bxSlider({
        responsive: true,
        minSlides: 2,
        maxSlides: 10,
        slideWidth: 480,
        preloadImages: 'all',
        easing: null,
        pager: false,
        controls: true,
        randomStart: false,
        nextSelector: this.next,
        prevSelector: this.prev,
        nextText: '',
        prevText: '',
        auto: true,
        pause: 15000,
        speed: 400,
        moveSlides: 2,
        autoHover: true,
        buildPager: function() {
          return '';
        }
      });
    } else {
      return this.slider.bxSlider({
        responsive: true,
        minSlides: 2,
        maxSlides: 10,
        slideWidth: 480,
        easing: 'ease-in-out',
        preloadImages: 'all',
        pager: false,
        controls: true,
        randomStart: false,
        nextSelector: this.next,
        prevSelector: this.prev,
        nextText: '',
        prevText: '',
        auto: true,
        pause: 15000,
        speed: 400,
        moveSlides: 2,
        autoHover: true,
        buildPager: function() {
          return '';
        }
      });
    }
  };

  return mainSlider;

})(BB.Widget);
